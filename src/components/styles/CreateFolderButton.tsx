import styled from 'styled-components'
import Icon from './common/Icon'

const CreateFolderButton = styled(Icon)`
  position: absolute;
  font-size: 32px;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`
export default CreateFolderButton
