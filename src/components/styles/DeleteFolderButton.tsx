import styled from 'styled-components'
import Icon from './common/Icon'

const DeleteFolderButton = styled(Icon)`
  position: absolute;
  font-size: 20px;
  right: 15px;
  top: 25px;
`

export default DeleteFolderButton
