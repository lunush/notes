import styled from 'styled-components'

const SidebarListItemCounter = styled.div`
  position: absolute;
  left: 10px;
  bottom: 12px;
  font-size: 10px;
  color: ${({ theme }) => theme.textSecondary};
`

export default SidebarListItemCounter
